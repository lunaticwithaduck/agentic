---
title: Implement agentic versioning (v0.1.0)
status: done
created: 2026-03-12
completed: 2026-03-12
---

## What

Add semver versioning to agentic so installed clients know what version they have
and future updates are clearly communicated.

## Acceptance Criteria

- [x] `VERSION` file at repo root (single source of truth, value: `0.1.0`)
- [x] `CHANGELOG.md` at repo root with v0.1.0 entry
- [x] `build.sh` stamps `VERSION` content into `ship/claude-code/.version` and `ship/cursor/.version`
- [x] `/status` command reads and displays the installed version
- [x] `ship/` rebuilt so `.version` files are present in both targets

## Outcome

Completed on 2026-03-12. Added `VERSION` file (`0.1.0`) as the single source of truth.
`buildScripts/build.sh` reads it and stamps `.version` into both `ship/claude-code/` and
`ship/cursor/` at the end of every build. `CHANGELOG.md` documents the v0.1.0 release with
the full feature set and known gaps. `/status` command updated to display the installed version
in the report header.
