---
title: Implement session continuity — Stop hook + SESSION.md + CONTEXT.md refactor
created: 2026-03-15
completed: 2026-03-15
status: done
---

## Goal
Replace the single CONTEXT.md monolith with a stable/volatile split:
- CONTEXT.md → stable architecture reference (~150 lines, rarely changes)
- SESSION.md → volatile session state (auto-generated by Stop hook, committed to repo)
- post-stop.cjs → writes SESSION.md with mechanical state + preserves Claude's session notes

## Steps
- [x] Implement post-stop.cjs (Stop hook writes SESSION.md)
- [x] Write initial SESSION.md capturing current state
- [x] Refactor CONTEXT.md — strip volatile sections, keep stable reference
- [x] Update CLAUDE.md — session start + session end instructions

## Outcome

Completed on 2026-03-15. Implemented a stable/volatile split for session context:

- **`post-stop.cjs`**: Stop hook now writes `SESSION.md` with open tasks, last 7 completions,
  and timestamp on every response. Preserves Claude-authored Session Notes if present
  (only overwrites the placeholder). Tested and runs clean.

- **`SESSION.md`**: New committed file at repo root. Volatile session state that the Stop hook
  maintains. Claude reads it at session start and updates `## Session Notes` before ending.
  55 lines when hook-generated; expands with Claude's notes.

- **`CONTEXT.md`**: Refactored from 303 → 162 lines. Removed all volatile sections
  (open issues, per-session state, in-flight tasks, Suite 04 per-task table). Now a stable
  architecture reference covering: core loop, multi-platform ship, hook architecture,
  benchmark suite, key file paths, workflow convention.

- **`CLAUDE.md`**: Added `## Session Continuity` section with explicit start/end instructions.
  Updated Stop hook description from "placeholder" to actual behavior.

**Design decisions:**
- SESSION.md is committed (not gitignored) so state persists across machines and team members
- Note preservation: hook regex matches Claude-written notes vs placeholder, only overwrites placeholder
- CONTEXT.md split rationale: stable content rarely changes → small, always readable;
  volatile content changes every session → lives in SESSION.md, always fresh

## .sc Evaluation

- Technologies/frameworks touched: Node.js/CommonJS filesystem scripting, Claude Code hook system
- Domain-specific knowledge: Claude Code Stop hook constraints (runs after response, reads files
  but can't re-invoke Claude), context rot research findings (200-line effective limit),
  note-preservation regex pattern for markdown section extraction
- Verdict: GENERATE
- Reason: Concrete patterns for Claude Code hook-based memory management — hook lifecycle
  constraints, what can/can't be generated automatically vs. what requires Claude to write.
